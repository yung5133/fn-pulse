# 这些版本组合已通过端到端验证。
# 注意 fastapi >= 0.141 起 TemplateResponse 只接受具名参数 (request=, name=, context=)，
# 任意升级 fastapi/starlette 前请先回归页面渲染。
fastapi==0.141.1
starlette==1.7.0
uvicorn==0.53.0
jinja2==3.1.6
requests==2.34.2
python-multipart==0.0.32
itsdangerous==2.2.0
# 企业微信智能机器人长连接的传输层（协议层对其懒加载，
# 未安装也不会影响其余功能的导入与测试）
websocket-client==1.9.2
